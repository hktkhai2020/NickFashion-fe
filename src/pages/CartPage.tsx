import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const CartPage: React.FC = () => {
  return (
    <div style={{ padding: '24px' }}>
      <Title level={2}>Cart Page</Title>
    </div>
  );
};

export default CartPage;
