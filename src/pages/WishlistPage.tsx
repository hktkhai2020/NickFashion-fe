import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const WishlistPage: React.FC = () => {
  return (
    <div style={{ padding: '24px' }}>
      <Title level={2}>Wishlist Page</Title>
    </div>
  );
};

export default WishlistPage;
