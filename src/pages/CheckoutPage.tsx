import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const CheckoutPage: React.FC = () => {
  return (
    <div style={{ padding: '24px' }}>
      <Title level={2}>Checkout Page</Title>
    </div>
  );
};

export default CheckoutPage;
