import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const RegisterPage: React.FC = () => {
  return (
    <div style={{ padding: '24px' }}>
      <Title level={2}>Register Page</Title>
    </div>
  );
};

export default RegisterPage;
