import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const ShopPage: React.FC = () => {
  return (
    <div style={{ padding: '24px' }}>
      <Title level={2}>Shop Page</Title>
    </div>
  );
};

export default ShopPage;
